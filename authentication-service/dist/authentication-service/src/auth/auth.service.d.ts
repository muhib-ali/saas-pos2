import { JwtService } from "@nestjs/jwt";
import { Repository } from "typeorm";
import { User } from "../entities/user.entity";
import { OauthToken } from "../entities/oauth-token.entity";
import { RolePermission } from "../entities/role-permission.entity";
import { Permission } from "../entities/permission.entity";
import { Module } from "../entities/module.entity";
import { Role } from "../entities/role.entity";
import { LoginDto } from "../../../api-gateway/src/auth/dto/login.dto";
import { RefreshDto } from "../../../api-gateway/src/auth/dto/refresh.dto";
import { CacheService } from "../cache/cache.service";
import { AppConfigService } from "../config/config.service";
import { ApiResponse } from "../common/interfaces/api-response.interface";
export declare class AuthService {
    private userRepository;
    private tokenRepository;
    private rolePermissionRepository;
    private permissionRepository;
    private moduleRepository;
    private roleRepository;
    private jwtService;
    private cacheService;
    private configService;
    private readonly logger;
    constructor(userRepository: Repository<User>, tokenRepository: Repository<OauthToken>, rolePermissionRepository: Repository<RolePermission>, permissionRepository: Repository<Permission>, moduleRepository: Repository<Module>, roleRepository: Repository<Role>, jwtService: JwtService, cacheService: CacheService, configService: AppConfigService);
    private getModulesWithPermissions;
    private getUserPermissions;
    login(loginDto: LoginDto, tenantId?: string): Promise<ApiResponse<any>>;
    refresh(refreshDto: RefreshDto): Promise<ApiResponse<any>>;
    logout(token: string): Promise<ApiResponse<null>>;
    validateToken(token: string, userId: string): Promise<User | null>;
}
